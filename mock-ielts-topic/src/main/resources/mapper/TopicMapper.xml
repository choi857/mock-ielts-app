<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apps.topic.mapper.TopicMapper">
    
    <resultMap id="BaseResultMap" type="com.apps.topic.model.entity.Topic">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="author" property="author"/>
        <result column="is_public" property="isPublic"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, content, author, is_public, create_time, update_time, deleted
    </sql>

    <select id="selectPublicTopics" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM topic
        WHERE is_public = true AND deleted = false
        ORDER BY create_time DESC
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM topic
        WHERE author = #{username} AND deleted = false
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM topic
        WHERE id = #{id} AND deleted = false
    </select>

    <insert id="insert" parameterType="com.apps.topic.model.entity.Topic" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO topic (
            title, content, author, is_public, create_time, update_time, deleted
        ) VALUES (
            #{title}, #{content}, #{author}, #{isPublic}, #{createTime}, #{updateTime}, #{deleted}
        )
    </insert>

    <update id="update" parameterType="com.apps.topic.model.entity.Topic">
        UPDATE topic
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="isPublic != null">is_public = #{isPublic},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id} AND deleted = false
    </update>

    <update id="deleteById">
        UPDATE topic SET deleted = true, update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper> 