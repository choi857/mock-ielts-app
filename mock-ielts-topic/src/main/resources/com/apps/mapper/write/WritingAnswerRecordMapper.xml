<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apps.mapper.write.WritingAnswerRecordMapper">

    <!-- 插入记录 -->
    <insert id="insert" parameterType="com.apps.model.write.WritingAnswerRecord">
        INSERT INTO COL_WRITE_USER_ANSWER_RECORD (
            record_id, user_id, task1_id, task2_id, created_at, updated_at
        ) VALUES (
            #{recordId}, #{userId}, #{task1Id}, #{task2Id}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <!-- 根据ID查询记录 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultType="com.apps.model.write.WritingAnswerRecord">
        SELECT
            record_id AS recordId, user_id AS userId, task1_id AS task1Id, task2_id AS task2Id, created_at AS createdAt, updated_at AS updatedAt
        FROM
            COL_WRITE_USER_ANSWER_RECORD
        WHERE
            record_id = #{recordId}
    </select>
</mapper>